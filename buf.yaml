version: v2
breaking:
  use:
    - FILE
lint:
  service_suffix: Controller
  use:
    - STANDARD
modules:
  - path: proto # the root folder for proto files.
    name: buf.build/grpc-boot-starter/lint
deps:
  - buf.build/bufbuild/protovalidate
  #- buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway